type Query {
  countries: [Country]
  country(code: ID!): Country
}

type Country {
  awsRegion: String!
  capital: String
  code: ID!
  currencies: [String!]!
  currency: String
  emoji: String!
  emojiU: String!
  name: String
  native: String!
  phone: String!
  phones: [String!]!
}
